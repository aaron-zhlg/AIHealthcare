[project]
name = "aihealthcare"
version = "0.1.0"
description = "AIHealthcare — ABIDE brain connectivity and ASD classification"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
]
